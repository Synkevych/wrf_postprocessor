&io_nml
  infile  = "test.nc"  ! WRF input file
  grid_outfile = "grid.data"  ! grid file output
  pmsl_outfile = "pmsl_"  ! mean sea level pressure
  ntimes1 = 28  ! number of time steps to process
/
